using UnityEngine;


// TODO -?
namespace BlockPuzzle.Scripts.Runtime.utilities {
	public class ApplicationStatusMonitor : MonoBehaviour {
		// public Subject <bool> AppPaused {get;} = new Subject <bool>();

		// TODO what
		private void OnApplicationPause (bool pauseStatus) {
			// AppPaused.OnNext(pauseStatus);
		}
	}
}
